{
  "name": "no-daal-chawal",
  "version": "1.0.0",
  "description": "Escape the boring meal rut",
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "postinstall": "npm run install:all && npm run build && cd server && npx prisma generate"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
